// 원시 타입 : number, string, boolean, null, undefined

// 타입 주석 (Type Annotation)
const num: number = 1
const numA: number = null // strictNull 옵션
const string: string = 'lorem '
const boolean: boolean = false
const isNull: null = null
const isUndefined: undefined = undefined

// Literal Type: 값으로 만든 타입
const numB: 10 = 10
const stringA: 'header' | 'footer' | 'body' = 'header'
